rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all users to their focus tasks, urges and progress
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
